# Runtime secrets come from ../../platform/.env when using Docker Compose.
# Set these only when running the crawler independently.
CRAWLER_DB_PASSWORD=
CRAWLER_DATABASE_URL=
REDIS_PASSWORD=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
DJANGO_SECRET_KEY=
CRAWLER_PROXY_URL=
